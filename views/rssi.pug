extends layout

block content
    ul
        each device in deviceRssi
            li(style="display: inline-block; vertical-align:top; padding-left: 20px; padding-right: 20px") #{device.device}
                each record in device.records
                    p #{record.rssi}
